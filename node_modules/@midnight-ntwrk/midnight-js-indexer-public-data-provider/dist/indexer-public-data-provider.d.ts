import type { PublicDataProvider, UnshieldedBalances, UnshieldedUtxos } from '@midnight-ntwrk/midnight-js-types';
import * as ws from 'isomorphic-ws';
import { type ContractBalance } from './gen/graphql';
export type IndexerUtxo = {
    owner: string;
    intentHash: string;
    tokenType: string;
    value: string;
};
export declare const toUnshieldedUtxos: (createdUtxo: readonly IndexerUtxo[], spentUtxo: readonly IndexerUtxo[]) => UnshieldedUtxos;
export declare const toUnshieldedBalances: (contractBalances: readonly ContractBalance[]) => UnshieldedBalances;
/**
 * Constructs a {@link PublicDataProvider} based on an {@link ApolloClient}.
 *
 * @param queryURL The URL of a GraphQL server query endpoint.
 * @param subscriptionURL The URL of a GraphQL server subscription (websocket) endpoint.
 * @param webSocketImpl An optional websocket implementation for the Apollo client to use.
 *
 * TODO: Re-examine caching when 'ContractCall' and 'ContractDeploy' have transaction identifiers included.
 */
export declare const indexerPublicDataProvider: (queryURL: string, subscriptionURL: string, webSocketImpl?: typeof ws.WebSocket) => PublicDataProvider;
//# sourceMappingURL=indexer-public-data-provider.d.ts.map